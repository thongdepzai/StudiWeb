export const customerState$ =(state) => state.customer.data;

export const videoState$ = (state) => state.videos.data;

export const dictionary$ = (state) => state.dictionarys.data;

export const modalState$ = (state) => state.modal;

export const comment$ = (state) => state.comments.data;

export const blog$ = (state) => state.blogs.data;
